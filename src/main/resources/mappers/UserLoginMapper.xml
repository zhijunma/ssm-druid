<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cn.school.mapper.mzj.UserLoginMapper">
    <!--用户注册-->
    <insert id="addUserInfo">
        <selectKey keyProperty="ds.userId" order="AFTER" resultType="java.lang.Long">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO user_info(
        user_password,
        mobile_phone,
        add_time
        )
        VALUES
        (
        #{ds.userPassword},
        #{ds.mobilePhone},
        #{ds.addTime}
        )
    </insert>
    <!-- 通过用户账号获取密码 -->
    <select id="getUserByMobilePhone" resultType="com.cn.school.entity.mzj.DSUserInfo">
        SELECT
            user_id as userId,
            user_password as userPassword
        FROM
            user_info
        WHERE
        delete_flag = 0
        and mobile_phone = #{mobilePhone}
    </select>
</mapper>